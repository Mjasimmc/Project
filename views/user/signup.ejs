<%- include('../layout/header.ejs') %>
    <%- include('../layout/before-login.ejs') %>


        <head>
            <link rel="stylesheet" href="\stylesheets\signIn.css">
        </head>
        <!--================Login Box Area =================-->

        <div class="wrapper fadeInDown">
            <div id="formContent">
                <!-- Tabs Titles -->

                <!-- Icon -->
                <!-- Login Form -->

                <form class="p-t-15" method="post" action="/register">
                    <% if (alertMessage) { %>
                        <div class="alert alert-success" role="alert">
                            <%= alertMessage %>
                        </div>
                        <% } %>

                            <h4>Login Your Account</h4>
                            <input type="text" class="fadeIn second" name="name" placeholder="Username" required>
                            <input type="email" class="fadeIn third" name="email" placeholder="Email" required>
                            <input type="tel" class="fadeIn fourth" minlength="10" maxlength="10" name="mobile"
                                placeholder="Phone" required>
                            <input type="password" class="fadeIn fifth" name="password" placeholder="Password" required>
                            <input type="password" class="fadeIn second" name="repassword" placeholder="Re-Password"
                                required>
                            <input type="submit" class="fadeIn fourth" value="Sign Up">
                </form>


            </div>
        </div>
        <style>
            input[type=text],
            input[type=email],
            input[type=password],
            input[type=tel] {
                background-color: #f6f6f6;
                border: none;
                color: #0d0d0d;
                padding: 15px 32px;
                text-align: center;
                text-decoration: none;
                display: inline-block;
                font-size: 16px;
                margin: 5px;
                width: 85%;
                border: 2px solid #f6f6f6;
            }

            input[type=text]:focus,
            input[type=email]:focus,
            input[type=password]:focus,
            input[type=tel]:focus {
                background-color: #fff;
                border-bottom: 2px solid #5fbae9;
            }
        </style>
        <%- include('../layout/footer.ejs') %>